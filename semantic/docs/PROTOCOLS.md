# Protocol models, rules and scientific limits

Protocol module version 1.0. Field spellings are taken from the pinned Wireshark dissector registrations, not independent packet parsers. See `extraction/Normalizer.cpp` for the finite mapping. Specification families are IEC 60870-5-104 and its IEC 60870-5-101 ASDU definitions, IEC 61850-8-1 GOOSE/MMS mappings, IEC 61850-9-2 sampled values, Modbus Application Protocol/TCP implementation guidance, and IEEE 1815 DNP3. No claim of complete standards conformance is made. Normative subsection numbers are intentionally not invented where licensed specification text was not available.

Every emitted finding includes its rule ID, occurrence ID, category, severity, frames, decoded-field evidence, expected condition, observed condition, deterministic explanation and basis. The following table defines the implemented families; the exact executable predicates and per-rule explanation are in the small protocol modules. Configured policy rules are separate from protocol observations. No built-in rule declares an attack.

| Module / reference fields | Correlation / rules | Assumptions and legitimate alternatives |
|---|---|---|
| IEC104: `iec60870_104.type`, `.tx`, `.rx`, `.utype`; `iec60870_asdu.typeid`, `.causetx`, `.addr`, `.ioa` | TCP stream, initiator, TypeID, CA, IOA, select/execute. Activation (6), confirmation (7), termination (10), negative/error response. Sequence continuity modulo 32768; acknowledgement regression uses half-range comparison. U-format STARTDT/STOPDT/TESTFR pairing and data after observed STOPDT. Duplicate operation and echoed-value mismatch. | Captures can omit confirmations, directions or previous STARTDT. Retries and resets are not attacks. Reordered observations do not update simple sequence expectations. Select confirmations complete a selection, not an operate action. Protocol measurements carry no inferred engineering unit. |
| GOOSE: `goose.appid`, `.gocbRef`, `.datSet`, `.stNum`, `.sqNum`, `.confRev`, `.timeAllowedtoLive` | Logical identity = APPID, control block, destination; MAC retained as observed attribute. stNum decrease, sqNum non-progression, first observed new-state sqNum, publisher change, revision/dataset/VLAN/simulation/commissioning changes, TTL gap between observed publications. | Publisher restart, redundancy, duplicate capture, reconfiguration, unobserved first retransmission or capture gaps remain possible. Max stNum to 1 is treated as wrap. Observing a new state first at sqNum >0 does not prove incorrect publisher behavior. Absence beyond capture end is not a TTL finding. |
| SV: `sv.appid`, `.svID`, `.smpCnt`, `.confRev`, `.smpSynch`, `.smpRate` | Logical stream/publisher tracking; duplicate or discontinuous sample counter, publisher/attribute changes; configured sampling interval and rate checks. | A deployed profile supplies counter modulus. With modulus 0 (unknown), a decrease is an unresolved counter observation and can be legitimate wrap. smpRate units depend on smpMod/profile. Unknown profile is never silently replaced with 4000/4800/65536. |
| MMS: `mms.invokeID`, `mms.confirmedServiceRequest`, `mms.confirmedServiceResponse` | Stream, initiator and invokeID; concurrent request-response/error pairing, ID reuse and service mismatch; identify/read/write operation categories. | This module observes confirmed service envelopes. It does not implement full association negotiation, every MMS service's object semantics or SCL type interpretation. Missing request is capture-boundary limited. Exported-PDU tests verify MMS envelopes, not a full OSI association. |
| Modbus: `mbtcp.trans_id`, `.unit_id`, `modbus.func_code`, `.request_frame`, register fields | TCP stream, initiator, TID and unit; response/exception pairing, reuse, function mismatch, write echo, decoded register observations/changes. | Direction follows the actual Wireshark mbtcp TCP-port preference and generated request association. Ambiguous port roles remain unresolved. Register quantities and values come from Wireshark; no independent byte parser or inferred float/unit conversion. |
| DNP3: `dnp3.al.func`, `.seq`, `.fir`, `.fin`, `.uns`, `.con`, `.iin`, `.obj` | TCP/endpoint and link-address circuit, application sequence, request/response fragments; unsolicited-response confirmation, sequence reuse, internal indications, control classification. | Basic application correlation; not all DNP3 object variations, secure authentication, select-before-operate profiles or transport error recovery. Internal indications are not uniformly faults. Missing capture fragments or retries make correlation uncertain. |

## Explicit IEC104 operation states

`COMMAND_ACTIVATED → COMMAND_CONFIRMED → COMMAND_TERMINATED` records a transition's previous/current state, triggering event/frame/time and expected next transition. Negative responses end the operation separately. A termination without observed confirmation retains `INCOMPLETE_UNKNOWN` and a missing-confirmation observation. Missing activation retains `INCOMPLETE_CAPTURE_BOUNDARY`. A configured deadline may expire only when later captured timestamps establish an observation window beyond that deadline. It does not establish why a response is missing.

Transfer control is a separate connection state: UNKNOWN, STARTED, STOPPED. A capture beginning with I-format traffic has UNKNOWN transfer state; the analyzer does not invent an observed TCP handshake or STARTDT. Measurement observations are complete observations, not fictitious request-response transactions. Their temporal relationship is constructed by measurement identity in the context builder.

## Extraction and provenance

Multi-PDU IEC104 and multiple IOAs remain separate events. MMS envelopes are scoped by their tree marker ancestors. SV ASDUs are scoped by their own svID tree ancestry. Dataset members are indexed observations; no engineering identity or physical units are invented without configuration.

Exported-PDU endpoints use the visible `exported_pdu.ipv4_src`/`ipv4_dst` (or IPv6) fields; hidden convenience aliases remain excluded. Events missing endpoint identity are retained as unresolved observations and do not enter cross-packet correlation.

Each copied scalar retains originating frame, exact Wireshark abbreviation, local tree node ID, data-source identity/name, source-relative offset/length and generated status. Source-relative offsets are **not necessarily current-frame offsets**. Reassembled sources are named and marked as additional sources, without claiming original-frame contiguity. Packet timestamps use Wireshark's time relative to the first frame, independent of display time-reference changes. No pointer to transient dissection data survives the callback.

SV `seqData` is opaque unless Wireshark decodes it. The plugin does not force the `sv.decode_data_as_phsmeas` preference or assign a measurement layout. Users may enable that Wireshark profile-specific preference when it matches their engineering configuration. Only then can its decoded measurement and quality fields enter the event model. Current generic SV fixtures validate counters/stream metadata; arbitrary waveform interpretation is outside scope.

## Extending the platform

Add a `ProtocolAnalyzer::consume(Event, Engine)` implementation and factory. Register finite Wireshark field mappings, preserve repeated object/PDU scope in normalization, define correlation keys and boundary behavior, and add independent synthetic wire fixtures. Rules must state their assumptions and alternatives. Adding an extractor field does not add it to the outbound allowlist: that is a separate reviewed change with privacy regression tests. A future renderer can consume events, transactions, transitions, findings and contexts without linking EPAN or the provider library.
